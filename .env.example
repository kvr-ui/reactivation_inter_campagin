# Copy to .env for local development. In production these live in
# Vercel → Project → Settings → Environment Variables (never in the repo).
MONGO_URL=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/reactivation?retryWrites=true&w=majority
DB_NAME=reactivation
